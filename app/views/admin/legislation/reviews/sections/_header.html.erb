<% provide :title do %>
  <%= t("admin.header.title") %> - <%= t("admin.menu.legislation") %> - <%= @process.title %> - <%= t("admin.legislation.reviews.sections.title") %>
<% end %>

<%= back_link_to(
  admin_legislation_review_path(@review), t("shared.back")
) %>

<h2><%= @process.title %></h2>

<%= render "admin/legislation/processes/subnav",
  process: @process, active: "reviews"
%>
